<div class="row">
  <div class="col-6 mx-5">
    <p>Title:{{name}}</p>
    <p>Posted by: {{user.name}}  on {{format_date date_created}}</p>
    <p>{{description}}</p>
  

{{#if logged_in}}
<div>
  <button>Leave Comment</button>
</div>
{{/if}}
</div>

<div class="col-4" style="visibility:hidden">
      <form class="form comment-form" style="visibility:hidden">
      <div class="form-group">
        <label for="comment-body">Comment:</label>
        <input class="form-input" type="text" id="comment-body" />
      </div>
      
      <div class="form-group">
        <button class="btn btn-primary" type="submit">Create Comment</button>
      </div>
    </form>

</div>

<script src="/js/comment.js"></script>
</div>


</div>

